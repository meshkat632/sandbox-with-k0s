{{- if .Values.ingress.enabled }}
Served through the {{ .Values.ingress.className }} ingress at {{ .Values.ingress.host | default "<any host>" }}{{ .Values.ingress.path }}
{{- else }}
kubectl -n {{ .Release.Namespace }} port-forward svc/{{ include "nginx-hello-world.fullname" . }} 8080:{{ .Values.service.port }}
then open http://localhost:8080
{{- end }}
